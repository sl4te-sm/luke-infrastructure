apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kubernetes-dashboard-web
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`kube.services.luke-domain.com`)
      services:
        - name: kubernetes-dashboard-web
          port: 8000
