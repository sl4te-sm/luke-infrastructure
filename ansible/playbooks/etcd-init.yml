- name: Install etcd hosts
  hosts: databases
  tasks:
    - name: Install tar
      become: true
      ansible.builtin.dnf:
        name: tar
        state: latest

    - name: Install etcd
      become: true
      script: /home/luke/ansible/scripts/install_etcd.sh
